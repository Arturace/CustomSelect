{
  "name": "custom-select",
  "version": "0.1.0",
  "description": "A custom select element using the Custom Elements API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "select",
    "custom-element"
  ],
  "author": "Artur Madjidov",
  "license": "ISC",
  "dependencies": {
    "tsc": "^1.20150623.0"
  }
}
